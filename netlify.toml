[build]
  command = "gatsby build"
  publish = "public/"

[template]
  incoming-hooks = ["Ghost"]

[[redirects]]
  from = "/*"
  to = "/blog"
  status = 200
  force = true
  headers = {X-From = "Netlify"}